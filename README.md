# A2_COMP371

To set up OpenGL for this project, please follow the setup (setup.md).
Also, make sure to install GLM and have the MSYS2 compiler

To start the assignment (i.e. see the graphics), run the following command in your terminal: ./main.exe
